# 待修改

> 本文件用于记录确认后暂不实施、待统一处理的修改事项。
> 每条记录包含：修改目标、当前状态（文件:行号）、修改方案与连带影响。
> 新条目追加在文件末尾，编号递增；实施完成后从本文件移除并归档。

---

## 实施批次总览

> 全部 6 个批次已实施完成并归档（12 条 → 9 个实施单元）。

| 批次 | 内容 | 优先级 | 状态 |
| --- | --- | --- | --- |
| 1 | #10 + #11 同步子系统正确性 | P0 | ✅ 已归档 |
| 2 | #4、#5、#6 行为修复 | P0 | ✅ 已归档 |
| 3 | #7、#12 配置与体验 | P1 | ✅ 已归档 |
| 4 | #3 绑定项目（功能部分） | P1 | ✅ 已归档 |
| 5 | #1 + #2 操作面板样式 | P1 | ✅ 已归档 |
| 6 | #8 + #9 命令执行子系统 | P2 | ✅ 已归档 |
| 收尾 | README / PRD 文档同步 | — | ✅ 完成 |

### 合并 / 拆分说明

- 合并：#1+#2、#8+#9、#10+#11（12 条 → 9 个实施单元）；
- 拆分：#3 = 功能（批次 4）+ helpCard 按钮（批次 5）；
- helpCard 按钮行（L145–157）为 #1/#2/#3/#8 共同编辑点，按批次增量追加，避免重复编辑同一区域；
- 实施完成一条从本文件移除并归档（批次内可并行、批次间按序）。

---

## 已实施归档

> 完成条目从上方移除并归档于此（批次 → 条目 → 实施说明）。

### 批次 2（P0：行为修复）

- **#4 停止过渡卡保留已输出内容（方案 A）**：已实施。
  - `src/main.ts`：`AgentRun` 新增 `latestOutput` 字段（submit 初始化空串，onDelta 同步更新）；`stop()` 过渡卡内容改为 `run.latestOutput || '正在停止处理。'`——停止瞬间卡片从「执行中 + 预览」→「正在停止 Agent + 已输出内容」，内容全程不丢失。
- **#5 引用消息获取失败不再发送给 agent**：已实施。
  - `src/main.ts`：`promptWithReplyContext` 返回 `{ prompt, error? }`（获取失败 →「无法获取引用消息：<原因>」，内容为空 →「引用消息内容为空或不存在。」）；`runPrompt` 有 error 时直接 markdown 回复「无法处理引用消息：<原因>」，不建 run、不设 `inFlightFeishuRun`。
- **#6 compact 移除公告刷新 + 失败反馈**：已实施。
  - `src/main.ts`：`session.compact` 分支移除 `.then(updateAnnouncement)`；catch 中向群发送「Session 压缩失败：<原因>」（原因截断 200 字符）；成功保持安静；中止运行中任务为 pi SDK 既定行为，保持现状。

### 批次 3（P1：配置与体验）

- **#7 创建项目群默认群名 = basename(cwd) + YYMMDDHHmm（本地时间）**：已实施。
  - `src/main.ts`：新增 `defaultProjectName(cwd)`（`basename` 为空兜底 `project`）；`project.create.submit` 默认名替换原 UTC 的 `Pi · YYYY-MM-DD HH:mm`。
- **#12 删除 LARK_PI_STATUS_AUTO_COMPACTION，固定显示 `(auto)` 标记**：已实施。
  - `src/pi.ts`：删除 `piAutoCompaction` 常量，`(auto)` 固定跟随 `session.autoCompactionEnabled`；`LARK_PI_STATUS_ENABLED` 保留。`.env.example`、`README.md` 同步清理（`PRD.md` 重写时已为目标态）；全项目无 `AUTO_COMPACTION` 残留。

---

### 批次 6（P2：命令执行子系统）

- **#8 常驻任务（后台任务）支持**：已实施。
  - `src/cards.ts`：`commandFormCard` 超时输入框 `default_value: '10'`；新增「常驻任务」checker 组件（`name: 'isBackground'`，飞书卡片 JSON 2.0，客户端 V7.9+）；helpCard 第三组追加「后台任务」按钮；新增 `bgTaskListCard`（命令 + 启动时间 + 停止按钮，空态提示）；
  - `src/main.ts`：新增 `BackgroundTask` 类型与 `backgroundTasks` Map；`cardFormFlag()` 读取 checker 布尔值；`command.submit` 勾选常驻任务时忽略超时；`runShellCommand` 后台模式（spawn 成功即完成「后台任务已启动」卡，注册到 `backgroundTasks`，不进入 `commandTasks`，输出丢弃）；`bgTask.form` / `bgTask.stop` 分支（位于绑定检查之前，未绑定群可用）；`shutdown` 终止全部后台任务；
  - `src/pi.ts`：`PiSessions` 构造注入 `backgroundTaskCountProvider`；`statusFor` 追加「后台任务 ×N」（仅 N>0）；`statusAt` 不显示。
- **#9 命令输出改为流式更新，去掉「查看输出」按钮**：已实施。
  - `src/cards.ts`：`commandRunningCard` 移除「查看输出」按钮，仅保留「停止」；
  - `src/main.ts`：移除 `command.output` 分支；新增 `createThrottledUpdate`（事件驱动节流，`COMMAND_CARD_UPDATE_INTERVAL_MS = 750`）；stdout/stderr 累计输出按节流原位更新执行中卡片；停止/超时/结束时最终卡 = 已流式显示的累计输出 + 追加状态消息（「正在停止命令。」/「命令已手动停止。」/「命令超时（N 秒）并已停止。」等）；
  - `terminateProcessGroup` 抽取为模块级函数（进程组 SIGTERM → 5s SIGKILL，以 `exitCode === null` 判断 close 后不再兜底），普通任务与后台任务共用；
  - 验证：节流行为（首次立即、突发合并、cancel）与卡片结构（checker/default_value/中文按钮/无查看输出/bgTask 列表）测试全部通过。
- **#1/#2/#3 条目**：批次 4/5 实施时归档记录已写入，本批将正文条目一并移除。

---

### 批次 4（P1：绑定项目功能）

- **#3 新增 bindProject「绑定项目」命令（功能部分）**：已实施。
  - `src/cards.ts`：新增 `bindProjectFormCard(baseCwd, bound)`（标题「绑定项目」/「修改绑定」，仅工作路径必填）；
  - `src/main.ts`：新增 `project.bind.form` / `project.bind.submit` 分支（位于绑定检查之前，未绑定群可用）；提交后 `state.update` 清空 `activeSessionFile/sessionSync/feishuOriginEntryIds/inFlightFeishuRun/announcementRevision` + flush + reconcile + updateAnnouncement；
  - 未绑定群发消息提示改为「请使用 `/help` 中的「绑定项目」」；
  - helpCard「绑定项目」按钮 → 批次 5 已一并加入（见下）。

### 批次 5（P1：操作面板样式）

- **#1 help 卡片功能按钮中文化 + #2 全部主色**：已实施。
  - `src/cards.ts` `helpCard`：7 个功能按钮改为中文（切换模型/切换思考强度/重命名会话/新建会话/压缩会话/切换会话/同步消息），全部 `primary`；未绑定提示「此群尚未绑定项目，请先绑定。」、未选 Session 提示「请使用「新建会话」或「切换会话」。」；第三组追加「绑定项目」按钮（#3 UI 部分）；`cmd` 全部不变；
  - `README.md`：操作面板按钮清单同步为中文名，补充「绑定项目」说明与默认群名规则。

---

### 批次 1（P0：同步子系统正确性）

- **#10 同步消息超长改为「截断 + 截断说明」（28 KB UTF-8 字节）**：已实施。
  - `src/main.ts`：新增 `SYNC_BODY_BYTE_LIMIT = 28 * 1024` 与 `SYNC_TRUNCATION_MARKER`；`syncComputerSessions` 改为按 `Buffer.byteLength` 判断、超限调用 `truncateSyncBody`（按字节预算 1/3:2/3 分配、按字符切分，多字节安全）；返回值 `tooLong` 改为 `truncated`；`session.sync.submit` toast 改为「已同步 N 轮对话（内容已截断）」。
- **#11 同步进度推进到已消费轮次（方案 B），feishuOriginEntryIds 降为即时标记**：已实施。
  - `src/main.ts`：抽出纯函数 `selectSyncTurns(entries, from, feishuOrigin, mode, count)`（飞书轮次排除但视为已消费、进度推进到最靠后的已消费轮次）；消费后清理进度之前的 `feishuOriginEntryIds`（O(1) 即时释放，`slice(-1000)` 保留兜底）；`types.ts` 注释同步更新。
  - 验证：`truncateSyncBody` 与 `selectSyncTurns` 的边界场景测试全部通过（含中文/emoji 多字节、临界字节、交错轮次、纯飞书轮次、半轮、ghost id 清理）。

---
